/- generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test_lists_freevars_1.ott -/




abbrev i := UNDEFINED
abbrev x := UNDEFINED

inductive p where
    | p_x : (_:x) -> p

  deriving Inhabited

inductive t where
    | t_x : (_:x) -> t
    | t_tuple : (_:List t) -> t
    | t_cond : (_:List (p × t)) -> t

  deriving Inhabited
open t p

/- - free variables - -/
mutual
def fv_p_t_list (p_t_list:List (p × t)) : List x :=
  match p_t_list with
  | [] => []
  | (p_,t_) :: p_t_list_ => (fv_t t_) ++ (fv_p_t_list p_t_list_)
def fv_t_list (t_list:List t) : List x :=
  match t_list with
  | [] => []
  | t_ :: t_list_ => (fv_t t_) ++ (fv_t_list t_list_)
def fv_t (t_5:t) :  List x :=
  match t_5 with
  | (t_x x) => [x]
  | (t_tuple (t_list)) => (fv_t_list t_list)
  | (t_cond (p_t_list)) => (fv_p_t_list p_t_list)
end



