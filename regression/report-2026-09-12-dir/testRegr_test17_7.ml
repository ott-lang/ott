(* generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test17.7.ott *)

type value_name = UNDEFINED
type index = UNDEFINED

type 
a = 
   A_var of value_name
 | A_let of ((value_name * a)) list * a


type 
b = 
   B_var of value_name
 | B_let of (value_name) list * a


(** substitutions *)
let rec msubst_a (sub: (value_name*a) list) (a__6:a) : a =
  match a__6 with
  | (A_var x) -> (try List.assoc x sub with Not_found -> (A_var x))
  | (A_let ((x_a_list),a)) -> A_let ((List.map (fun (x_,a_) -> (x_,(msubst_a sub a_))) x_a_list),(msubst_a (List.filter (fun (x_5,a_5) -> not(List.mem x_5 ((List.map (fun (x_,a_) -> x_) x_a_list)))) sub) a))


let rec subst_a (a_5:a) (x_5:value_name) (a__6:a) : a =
  match a__6 with
  | (A_var x) -> (if x=x_5 then a_5 else (A_var x))
  | (A_let ((x_a_list),a)) -> A_let ((List.map (fun (x_,a_) -> (x_,(subst_a a_5 x_5 a_))) x_a_list),(if List.mem x_5 ((List.map (fun (x_,a_) -> x_) x_a_list)) then a else (subst_a a_5 x_5 a)))


let bmsubst_b (sub: (value_name*b) list) (b_6:b) : b =
  match b_6 with
  | (B_var x) -> (try List.assoc x sub with Not_found -> (B_var x))
  | (B_let ((x_list),a)) -> B_let (x_list,a)


let bsubst_b (b5:b) (x_5:value_name) (b_6:b) : b =
  match b_6 with
  | (B_var x) -> (if x=x_5 then b5 else (B_var x))
  | (B_let ((x_list),a)) -> B_let (x_list,a)


let msubst_b (sub: (value_name*a) list) (b5:b) : b =
  match b5 with
  | (B_var x) -> B_var x
  | (B_let ((x_list),a)) -> B_let (x_list,(msubst_a (List.filter (fun (x_5,a5) -> not(List.mem x_5 (x_list))) sub) a))


let subst_b (a5:a) (x_5:value_name) (b5:b) : b =
  match b5 with
  | (B_var x) -> B_var x
  | (B_let ((x_list),a)) -> B_let (x_list,(if List.mem x_5 (x_list) then a else (subst_a a5 x_5 a)))




