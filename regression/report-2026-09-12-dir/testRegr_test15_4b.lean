/- generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test15.4b.ott -/




abbrev index := UNDEFINED
abbrev x := UNDEFINED

inductive a where
    | a_Ax : (_:x) -> a
    | a_a : (_:a) -> a
    | a_C : (_:List a) -> a
    | a_CC : (_:List a) -> a

  deriving Inhabited
open a
mutual
def aux_b_a_of_a_list (a_list:List a) : List x :=
  match a_list with
  | [] => []
  | a_ :: a_list_ => (aux_b_a_of_a a_) ++ (aux_b_a_of_a_list a_list_)
def aux_b_a_of_a (a_5:a) : List x :=
  match a_5 with
  | (a_Ax x) => [x]
  | (a_a a) => (aux_b_a_of_a a)
  | (a_C (a_list)) => (aux_b_a_of_a_list a_list)
  | (a_CC (a_list)) => (aux_b_a_of_a_list a_list)
end


/- - subrules - -/
mutual
def is_av_of_a_list (a_list:List a) : Bool :=
  match a_list with
  | [] => true
  | a_ :: a_list_ => ((is_av_of_a a_)) && (is_av_of_a_list a_list_)
def is_av_of_a (a_5:a) : Bool :=
  match a_5 with
  | (a_Ax x) => false
  | (a_a a) => (true)
  | (a_C (a_list)) => (is_av_of_a_list a_list)
  | (a_CC (a_list)) => (is_av_of_a_list a_list)
end


/- - free variables - -/
mutual
def ftv_a_list (a_list:List a) : List x :=
  match a_list with
  | [] => []
  | a_ :: a_list_ => (ftv_a a_) ++ (ftv_a_list a_list_)
def ftv_a (a_5:a) :  List x :=
  match a_5 with
  | (a_Ax x) => [x]
  | (a_a a) => (ftv_a a)
  | (a_C (a_list)) => (ftv_a_list a_list)
  | (a_CC (a_list)) => (ftv_a_list a_list)
end

/- - library functions - -/
def list_assoc [DecidableEq a] (l:List (a × b)) (x:a) : Option b :=
match l with
| [] => none
| (x',y')::t => if x=x' then some y' else list_assoc t x


/- - substitutions - -/
mutual
def msubst_a_list (sub:List (x×a)) (a_list:List a) : List a :=
  match a_list with
  | [] => []
  | a_ :: a_list_ => (msubst_a sub a_) :: (msubst_a_list sub a_list_)
def msubst_a (sub:List (x×a)) (a__6:a) : a :=
  match a__6 with
  | (a_Ax x) => (match list_assoc sub x with | none => (a_Ax x)| some a_5 => a_5 )
  | (a_a a) => a_a (msubst_a sub a)
  | (a_C (a_list)) => a_C (msubst_a_list sub a_list)
  | (a_CC (a_list)) => a_CC (msubst_a_list sub a_list)
end

mutual
def subst_a_list (a_5:a) (x5:x) (a_list:List a) : List a :=
  match a_list with
  | [] => []
  | a_ :: a_list_ => (subst_a a_5 x5 a_) :: (subst_a_list a_5 x5 a_list_)
def subst_a (a_5:a) (x5:x) (a__6:a) : a :=
  match a__6 with
  | (a_Ax x) => (if x=x5 then a_5 else (a_Ax x))
  | (a_a a) => a_a (subst_a a_5 x5 a)
  | (a_C (a_list)) => a_C (subst_a_list a_5 x5 a_list)
  | (a_CC (a_list)) => a_CC (subst_a_list a_5 x5 a_list)
end



