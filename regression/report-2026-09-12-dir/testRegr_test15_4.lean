/- generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test15.4.ott -/




abbrev index := UNDEFINED
abbrev x := UNDEFINED

inductive a where
    | a_Ax : (_:x) -> a
    | a_AA : a
    | a_a : (_:a) -> a
    | a_C : (_:List a) -> a
    | a_D : (_:List a) -> a
    | a_CC : (_:List a) -> a
    | a_DD : (_:List a) -> a

  deriving Inhabited
open a

/- - subrules - -/
mutual
def is_a_of_a_list (av_list:List a) : Bool :=
  match av_list with
  | [] => true
  | av_ :: av_list_ => ((is_av_of_a av_)) && (is_a_of_a_list av_list_)
def is_a_of_a (a_5:a) : Bool :=
  match a_5 with
  | (a_Ax x) => (true)
  | a_AA => (true)
  | (a_a a) => ((is_a_of_a a))
  | (a_C (a_list)) => (is_a_of_a_list a_list)
  | (a_D (av_list)) => (is_a_of_a_list av_list)
  | (a_CC (a_list)) => (is_a_of_a_list a_list)
  | (a_DD (av_list)) => (is_a_of_a_list av_list)
def is_av_of_a_list (av_list:List a) : Bool :=
  match av_list with
  | [] => true
  | av_ :: av_list_ => ((is_av_of_a av_)) && (is_av_of_a_list av_list_)
def is_av_of_a (a_5:a) : Bool :=
  match a_5 with
  | (a_Ax x) => false
  | a_AA => false
  | (a_a a) => ((is_a_of_a a))
  | (a_C (a_list)) => (is_av_of_a_list a_list)
  | (a_D (av_list)) => (is_av_of_a_list av_list)
  | (a_CC (a_list)) => false
  | (a_DD (av_list)) => false
end


/- - free variables - -/
mutual
def fv_a_list (a_list:List a) : List x :=
  match a_list with
  | [] => []
  | a_ :: a_list_ => (fv_a a_) ++ (fv_a_list a_list_)
def fv_a (a_5:a) :  List x :=
  match a_5 with
  | (a_Ax x) => [x]
  | a_AA => []
  | (a_a a) => (fv_a a)
  | (a_C (a_list)) => (fv_a_list a_list)
  | (a_D (av_list)) => []
  | (a_CC (a_list)) => (fv_a_list a_list)
  | (a_DD (av_list)) => []
end



