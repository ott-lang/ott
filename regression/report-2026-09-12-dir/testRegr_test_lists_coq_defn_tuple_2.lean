/- generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test_lists_coq_defn_tuple_2.ott -/




abbrev i := UNDEFINED

inductive foo where
    | foo_Foo : foo

  deriving Inhabited

inductive barbar where
    | barbar_Bar : barbar
    | barbar_Qux : barbar

  deriving Inhabited
open foo barbar

/- - subrules - -/
def is_bar_of_barbar (barbar5:barbar) : Bool :=
  match barbar5 with
  | barbar_Bar => (true)
  | barbar_Qux => false


/- definitions -/


/- defns J -/
mutual
inductive J_one : foo -> barbar -> Prop where    /- defn one -/

inductive J_hello : Prop where    /- defn hello -/
| J_hello_world: forall (foo_bar_list:List (foo × barbar)),
(List.all foo_bar_list (fun (foo_,bar_) => is_bar_of_barbar bar_)) ->
((∀ foo_bar_list_ ∈ foo_bar_list, (fun (foo_,bar_) => (J_one foo_ bar_)) foo_bar_list_))
 -> 
J_hello 


end


