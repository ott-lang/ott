/- generated by Ott 0.34 from: /home/pes20/claude-home/ott/regression/../tests/test18.1.ott -/




/-- term variable -/
abbrev termvar := Nat
abbrev index := Nat

mutual
inductive a : Type where
  | a_AA : a
  | a_C (_ : list_a) : a
  | a_D (x : termvar) (_ : list_a) : a
  deriving Inhabited
inductive list_a : Type where
  | Nil_list_a : list_a
  | Cons_list_a (_ : a) (_ : list_a) : list_a
  deriving Inhabited
end
open list_a a
def map_list_a {A : Type} (f : a -> A) (l : list_a) : List A :=
  match l with
  | Nil_list_a => []
  | Cons_list_a h tl_ => (f h) :: (map_list_a f tl_)

def make_list_a (l : List a) : list_a :=
  match l with
  | [] => Nil_list_a
  | h :: tl_ => Cons_list_a h (make_list_a tl_)

def unmake_list_a (l : list_a) : List a :=
  match l with
  | Nil_list_a => []
  | Cons_list_a h tl_ => h :: (unmake_list_a tl_)

def nth_list_a (n : Nat) (l : list_a) : Option a :=
  match n, l with
  | 0, Cons_list_a h tl_ => some h
  | 0, _ => none
  | _+1, Nil_list_a => none
  | k+1, Cons_list_a _ tl_ => nth_list_a k tl_

def app_list_a (l m : list_a) : list_a :=
  match l with
  | Nil_list_a => m
  | Cons_list_a h tl_ => Cons_list_a h (app_list_a tl_ m)


inductive list_termvar : Type where
    | Nil_list_termvar : list_termvar
    | Cons_list_termvar : termvar -> list_termvar -> list_termvar
  deriving Inhabited
open list_termvar

def map_list_termvar {A : Type} (f : termvar -> A) (l : list_termvar) : List A :=
  match l with
  | Nil_list_termvar => []
  | Cons_list_termvar h tl_ => (f h) :: (map_list_termvar f tl_)

def make_list_termvar (l : List termvar) : list_termvar :=
  match l with
  | [] => Nil_list_termvar
  | h :: tl_ => Cons_list_termvar h (make_list_termvar tl_)

def unmake_list_termvar (l : list_termvar) : List termvar :=
  match l with
  | Nil_list_termvar => []
  | Cons_list_termvar h tl_ => h :: (unmake_list_termvar tl_)

def nth_list_termvar (n : Nat) (l : list_termvar) : Option termvar :=
  match n, l with
  | 0, Cons_list_termvar h tl_ => some h
  | 0, _ => none
  | _+1, Nil_list_termvar => none
  | k+1, Cons_list_termvar _ tl_ => nth_list_termvar k tl_

def app_list_termvar (l m : list_termvar) : list_termvar :=
  match l with
  | Nil_list_termvar => m
  | Cons_list_termvar h tl_ => Cons_list_termvar h (app_list_termvar tl_ m)

/- definitions -/


/- defns Jtype -/
/-- defn Foo -/
inductive Foo : a → Prop where
  | Foo_1C : ∀ (a_list : list_a),
      Foo (a_C a_list)

  | Foo_1D : ∀ (a_list : list_a) (x : termvar),
      Foo (a_D x a_list)

  | Foo_2 : ∀ (a_list : list_a) (x_list : list_termvar),
      Foo (a_C (make_list_a (map_list_termvar (fun x_ => (a_D x_ a_list)) x_list)))



